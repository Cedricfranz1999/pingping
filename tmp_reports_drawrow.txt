const drawRow = (p: any) => {
        const colX = [pageMargin, pageMargin + cols[0], pageMargin + cols[0] + cols[1], pageMargin + cols[0] + cols[1] + cols[2], pageMargin + cols[0] + cols[1] + cols[2] + cols[3]];
       
